---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: govau/cga-cf-bosh-cli
    tag: latest

inputs:
- name: deploy-src

outputs: []

params:
  KUBECONFIG:
  TILLER_NAMESPACE: sentry-ci
  NAMESPACE:

run:
  path: deploy-src/ci/delete.sh
