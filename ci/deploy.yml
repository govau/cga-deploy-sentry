---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: govau/cga-cf-bosh-cli
    tag: latest

inputs:
- name: src
- name: deploy-src

outputs: []

params:
  KUBECONFIG:
  NAMESPACE:

run:
  path: deploy-src/ci/deploy.sh
